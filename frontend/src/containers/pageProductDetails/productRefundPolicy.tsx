import React from "react";
import styled from "styled-components";

const SContainer = styled.div``;

/////////////////////////////////////////////////////////////////////////

const CProductRefundPolicy = ({ product }) => {
  return (
    <SContainer>
      <h1>Refund policy</h1>
    </SContainer>
  );
};

export default CProductRefundPolicy;
